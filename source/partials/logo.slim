a.logo(href="/")

  - data.hs.SITE(0).each do |id, site|

    img.logo__image.logo__image--mobile(
      alt="#{ site.logo_mobile.description }"
      src="#{ site.logo_mobile.url }"
    )

    img.logo__image.logo__image--desktop(
      alt="#{ site.logo_desktop.description }"
      src="#{ site.logo_desktop.url }"
    )
