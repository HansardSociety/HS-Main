button.button{
  class="js-trigger-#{ scope }"
  class="#{ 'js-trigger-exclusive' if exclusive }"
  role="button"
  id="#{ aria_id.parameterize }"
  aria-controls="#{ aria_target_id.parameterize }"
  data-change-state="#{ js_change_state.parameterize }"
}

  - if type != 'menu'
    span{
      aria-hidden="true"
      class="button__#{ type }"
    }
      = yield
  - else
    span{
      aria-hidden="true"
      class="button__#{ type }"
    }
