button.button{
  class="#{ 'button--' + type if type != nil }"
  class="#{ 'button--' + size }"
  class="js-trigger-#{ state[:scope] }"
  class="#{ 'js-state-exclusive' if state[:excl] }"

  data-state-action="#{ 'js-' + state[:action].parameterize }"

  *{ "#{ 'data-state-page' if state[:page] }": "#{ 'js-page-' + state[:page] if state[:page] }" }

  aria-controls="#{ aria_target_id.parameterize }"
  role="button"
  id="#{ aria_id.parameterize }"
}
  /  Hello

  - if icon == 'menu'
    span{
      aria-hidden="true"
      class="button__#{ icon }"
    }

  - else
    span.button__content{
      aria-hidden="true"
      class="#{ 'button__content--' + animation if animation != nil }"
    }

      / Icon
      - if icon != nil
        = partial 'partials/icon',
          :locals => { :icon => icon,
                       :color => color,
                       :size => size }

      - if text != nil
        span.button__text{
          data-on-content="#{ text[:on] }"
          data-off-content="#{ text[:off] }"
        }
